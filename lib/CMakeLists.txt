# Copyright 2011,2012,2016,2018,2019 Free Software Foundation, Inc.
#
# This file was generated by gr_modtool, a tool from the GNU Radio framework
# This file is a part of gr-scopy
#
# SPDX-License-Identifier: GPL-3.0-or-later
#

########################################################################
# Setup library
########################################################################
include(GrPlatform) #define LIB_SUFFIX


include(FindFLEX REQUIRED)
find_package(BISON "3.0.2" REQUIRED)

flex_target(lexer
	${CMAKE_CURRENT_SOURCE_DIR}/iio_math_lexer.l ${CMAKE_CURRENT_BINARY_DIR}/lexer.c)
bison_target(parser
	${CMAKE_CURRENT_SOURCE_DIR}/iio_math_parser.y ${CMAKE_CURRENT_BINARY_DIR}/parser.c)

list(APPEND scopy_sources
	trapezoidal_impl.cc
	iio_math_impl.cc
	iio_math_gen_impl.cc
	iio_power_ff_impl.cc
	iio_modulo_ff_impl.cc
	iio_modulo_const_ff_impl.cc
	goertzel_scopy_fc_impl.cc
	goertzel_scopy.cc
	${FLEX_lexer_OUTPUTS}
	${BISON_parser_OUTPUTS}
    t_source_impl.cc
)

set(scopy_sources "${scopy_sources}" PARENT_SCOPE)
if(NOT scopy_sources)
    MESSAGE(STATUS "No C++ sources... skipping lib/")
    return()
endif(NOT scopy_sources)

add_library(gnuradio-scopy SHARED ${scopy_sources})
target_link_libraries(gnuradio-scopy gnuradio::gnuradio-runtime)
target_include_directories(gnuradio-scopy
    PUBLIC $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../include>
    PUBLIC $<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}> # for flex/bison includes
    PUBLIC $<INSTALL_INTERFACE:include>
  )
set_target_properties(gnuradio-scopy PROPERTIES DEFINE_SYMBOL "gnuradio_scopy_EXPORTS")

if(APPLE)
    set_target_properties(gnuradio-scopy PROPERTIES
        INSTALL_NAME_DIR "${CMAKE_INSTALL_PREFIX}/lib"
    )
endif(APPLE)

########################################################################
# Install built library files
########################################################################
include(GrMiscUtils)
GR_LIBRARY_FOO(gnuradio-scopy)

########################################################################
# Print summary
########################################################################
message(STATUS "Using install prefix: ${CMAKE_INSTALL_PREFIX}")
message(STATUS "Building for version: ${VERSION} / ${LIBVER}")

########################################################################
# Build and register unit test
########################################################################
include(GrTest)

# If your unit tests require special include paths, add them here
#include_directories()
# List all files that contain Boost.UTF unit tests here
list(APPEND test_scopy_sources
)
# Anything we need to link to for the unit tests go here
list(APPEND GR_TEST_TARGET_DEPS gnuradio-scopy)

if(NOT test_scopy_sources)
    MESSAGE(STATUS "No C++ unit tests... skipping")
    return()
endif(NOT test_scopy_sources)

foreach(qa_file ${test_scopy_sources})
    GR_ADD_CPP_TEST("scopy_${qa_file}"
        ${CMAKE_CURRENT_SOURCE_DIR}/${qa_file}
    )
endforeach(qa_file)
